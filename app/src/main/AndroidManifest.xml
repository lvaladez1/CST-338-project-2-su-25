<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CST338project2su25">
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddMediaActivity"
            android:exported="true" />
        <activity
            android:name=".DisplayMediaActivity"
            android:exported="false" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".FavoritesActivity"
            android:exported="false"/>
        <activity android:name=".FilterFavoritesActivity" />
        <activity android:name=".ReviewHistoryActivity" />
        <activity android:name=".MainActivity"
            android:exported="true"/>
        <activity android:name=".AdminCreateUserActivity" />
        <activity android:name=".AdminViewUsersActivity" />
        <activity android:name=".ReviewsActivity"
            android:exported="false"/>
    </application>

</manifest>